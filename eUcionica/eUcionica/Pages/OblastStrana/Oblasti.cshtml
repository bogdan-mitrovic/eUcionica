@page
@model eUcionica.Pages.OblastStrana.OblastiModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Oblasti</h1>

<p>
    <a asp-page="Create">Dodaj novu oblast</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Oblast[0].Predmet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oblast[0].Ime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Oblast) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Predmet.Ime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Uredi</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Izbrisi</a>
            </td>
        </tr>
}
    </tbody>
</table>
